# Global Claude Preferences

## Before Creating
- Always run repo names and project names by the user before creating them

## Commands
- Use `uv run python` not `python3` directly
- Use tmux sessions with `send-keys` for interactive scripts that require user input

## MCP Servers
- Always add a `reload()` tool that hot-reloads the server module for development
